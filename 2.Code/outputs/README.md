# Generated outputs

Tracked experiments are written to `outputs/runs/<run-id>/` and parameter studies to
`outputs/sweeps/`. These generated directories are intentionally ignored by Git; archive selected
run folders with the paper, report or release that uses them.
